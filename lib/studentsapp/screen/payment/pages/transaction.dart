import 'package:flutter/material.dart';

class StTransaction extends StatelessWidget {
  const StTransaction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
