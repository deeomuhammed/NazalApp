import 'package:flutter/material.dart';

class StPayment extends StatelessWidget {
  const StPayment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
