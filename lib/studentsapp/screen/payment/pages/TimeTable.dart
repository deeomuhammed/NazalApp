import 'package:flutter/material.dart';

class StTimeTable extends StatelessWidget {
  const StTimeTable({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
