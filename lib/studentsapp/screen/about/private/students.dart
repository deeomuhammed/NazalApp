import 'package:flutter/material.dart';

class StStudents extends StatelessWidget {
  const StStudents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(),
    );
  }
}
