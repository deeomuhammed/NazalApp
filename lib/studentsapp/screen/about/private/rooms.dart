import 'package:flutter/material.dart';

class StRooms extends StatelessWidget {
  const StRooms({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
